{
  "name": "calendar-transit-extension",
  "version": "1.0.0",
  "description": "Chrome extension for automatically creating transit events",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run build:popup && bun run build:background && bun run build:copy",
    "build:popup": "bun build popup/popup.ts --outdir=dist --target=browser --minify",
    "build:background": "bun build src/background.ts --outdir=dist --target=browser --minify",
    "build:copy": "cp manifest.json dist/ && cp -r icons dist/ && cp popup/popup.html dist/ && cp popup/popup.css dist/",
    "dev": "bun run build",
    "clean": "rm -rf dist",
    "test": "bun run src/cli/testRunner.ts",
    "test:json": "bun run src/cli/testRunner.ts --json",
    "test:execute": "bun run src/cli/testRunner.ts --execute",
    "test:unit": "bun test",
    "lint": "bunx @biomejs/biome check .",
    "lint:fix": "bunx @biomejs/biome check --write .",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun run lint"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/bun": "latest",
    "@types/chrome": "^0.1.32",
    "typescript": "^5.0.0"
  }
}
